name: CD

on:
  push:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  # Docker 이미지 빌드 및 푸시
  build:
    runs-on: ubuntu-latest
    outputs:
      profile: ${{ steps.set-profile.outputs.profile }}
      tag: ${{ steps.set-tag.outputs.tag }}

    steps:
      # 소스 코드 체크아웃
      - uses: actions/checkout@v4

      # 브랜치에 따라 실행 프로필 결정
      - name: Set profile
        id: set-profile
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "profile=prod" >> $GITHUB_OUTPUT
          else
            echo "profile=dev" >> $GITHUB_OUTPUT
          fi

      # 브랜치별 Docker 이미지 태그 결정
      - name: Set image tag
        id: set-tag
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "tag=prod" >> $GITHUB_OUTPUT
          else
            echo "tag=dev" >> $GITHUB_OUTPUT
          fi

      # buildx 세팅
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Docker Hub 로그인
      - name: Docker login
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # Docker 이미지 빌드 및 push
      - name: Build and push Docker image
        run: |
          IMAGE=${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}
          TAG=${{ steps.set-tag.outputs.tag }}

          docker buildx build \
            -f docker/Dockerfile \
            -t ${IMAGE}:${TAG} \
            --push .

  # self-hosted 서버에서 배포
  deploy:
    runs-on: self-hosted
    needs: build

    steps:
      # 레포지토리 체크아웃 (docker-compose 사용)
      - uses: actions/checkout@v4

      # docker-compose로 서비스 재배포
      - name: Deploy with Docker Compose
        env:
          PROFILE: ${{ needs.build.outputs.profile }}
          IMAGE_TAG: ${{ needs.build.outputs.tag }}
          ENV_FILE: ${{ secrets.ENV_FILE }}
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_REPO: ${{ secrets.DOCKER_REPO }}
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          NAVER_AD_API_KEY: ${{ secrets.NAVER_AD_API_KEY }}
          NAVER_AD_SECRET_KEY: ${{ secrets.NAVER_AD_SECRET_KEY }}
          NAVER_AD_CUSTOMER_ID: ${{ secrets.NAVER_AD_CUSTOMER_ID }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          KAKAO_API_KEY: ${{ secrets.KAKAO_API_KEY }}
        run: |
          set -e

          mkdir -p docker

          # 멀티라인 ENV_FILE 안전 저장
          printf "%s" "$ENV_FILE" > docker/.env
          echo "" >> docker/.env

          echo "PROFILE=${PROFILE}" >> docker/.env
          echo "IMAGE_TAG=${IMAGE_TAG}" >> docker/.env
          echo "DOCKER_USERNAME=${DOCKER_USERNAME}" >> docker/.env
          echo "DOCKER_REPO=${DOCKER_REPO}" >> docker/.env

          echo "KAKAO_REST_API_KEY=${KAKAO_REST_API_KEY}" >> docker/.env
          echo "NAVER_AD_API_KEY=${NAVER_AD_API_KEY}" >> docker/.env
          echo "NAVER_AD_SECRET_KEY=${NAVER_AD_SECRET_KEY}" >> docker/.env
          echo "NAVER_AD_CUSTOMER_ID=${NAVER_AD_CUSTOMER_ID}" >> docker/.env

          docker compose -f docker/docker-compose.yml --env-file docker/.env down
          docker compose -f docker/docker-compose.yml --env-file docker/.env pull
          docker compose -f docker/docker-compose.yml --env-file docker/.env up -d --build